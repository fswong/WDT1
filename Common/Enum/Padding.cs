using System;
using System.Collections.Generic;
using System.Text;

namespace Common.Enum
{
    // this probably should not use enumeration
    public enum Padding
    {
        id = 3,
        name = 30,
        quantity = 15,
        boolean = 5,
    }
}
